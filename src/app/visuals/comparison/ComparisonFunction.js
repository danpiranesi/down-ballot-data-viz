'use client';
import React, { useContext} from 'react';
import { VoteDataContext } from "../layout";


export default function ComparisonFunc () {
    const voteData = useContext(VoteDataContext);
    return (
       <div>
        The Comparison visual is not live yet
       </div>
    )
}


